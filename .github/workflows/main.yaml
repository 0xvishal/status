name: Deploy
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
       node-version: [16.x]
    steps:
      - uses: actions/checkout@master
        with:
          node-version: ${{ matrix.node-version }}

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Checkout Target Repository
        uses: actions/checkout@v3
        with:
          repository: 0xvishal/status
          path: infra/helm
          token: ${{ secrets.TOKEN }}

      # - name: Update Image Version in the related HelmChart values.yaml
      #   uses: OpsVerseIO/image-updater-action@0.1.0
      #   with:
      #     valueFile: 'infra/helm/qa.values.yaml'
      #     propertyPath: 'deployment.containers.version'
      #     value: 'v1.2.3'
      #     branch: main/v1.2.3
      #     targetBranch: main
      #     createPR: true
      #     message: 'Update Image Version to v1.2.3'

      # - name: Update Image Version in the related HelmChart values.yaml
      #   uses: OpsVerseIO/image-updater-action@0.1.0
      #   with:
      #     valueFile: 'infra/helm/qa.values.yaml'
      #     propertyPath: 'deployment.containers.version'
      #     value: 'v1.2.3'
      #     branch: main/v1.2.3
      #     targetBranch: main
      #     masterBranchName: main
      #     createPR: false
      #     message: 'Update Image Version to 1.2.3' 

      # - name: Update values.yaml
      #   run: pwd
      # - run: ls infra/helm  

      - name: Update values.yaml
        run: |      
          # cd infra/helm
          # cat infra/helm/qa.values.yaml
          # ls -al
          # sed -i 's|version:.*|version: 'v1.2.3'|' infra/helm/qa.values.yaml 
          touch test
          echo "this is test file " > test
          # git config --global user.name 'GitHub Actions'
          # git config --global user.email 'actions@github.com'
          git add .
          git commit -m "add file"
          git remote set-url origin https://0xvishal:${{ secrets.TOKEN }}@github.com/${{ github.repository }}
          git push 
          # git add qa.values.yaml
          # git commit -m "Update qa.values.yaml"
          # git push



          

